<tr>
  {{#each columns as |column|}}
    {{component (concat "table-header/" column.headerType)
      column=column
      table=table
      onClick=onHeaderClick
      onDoubleClick=onHeaderDoubleClick
    }}
  {{/each}}
</tr>
