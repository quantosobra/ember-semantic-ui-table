{{#each table.visibleColumns as |column|}}
  {{component
    (concat "table-cell/" column.cellType)
    column=column
    row=row
    index=index
    table=table
    onClick=onCellClick
    onDoubleClick=onCellDoubleClick
  }}
{{/each}}
