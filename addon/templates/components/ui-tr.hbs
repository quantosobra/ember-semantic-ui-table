{{#each table.visibleColumns as |column|}}
  {{component (concat "table-cell/" column.cellType)
    column=column
    row=row
    table=table
    onClick=onCellClick
    onDoubleClick=onCellDoubleClick
  }}
{{/each}}
